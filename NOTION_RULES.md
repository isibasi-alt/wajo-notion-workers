# Notion 鉄板ルール（全AI厳守 / DO NOT IGNORE）

最終更新: 2026-06-07

## 絶対ルール
1. **プロパティを勝手に増やさない。**
2. **データベースを勝手に増やさない。**
3. **ビューを勝手に増やさない／作らない。**

これらが必要なときは、**必ず事前に大ちゃん（石橋）の承認**を取ること。承認なしの追加は禁止。

## なぜ（理由）
Notionは**プロパティを1個追加するだけで、各ビューの「表示/非表示」と「並び順」が全リセット**される。
その復旧（優先プロパティを最上部へドラッグし直す等）は大ちゃんが最も嫌う手作業であり、過去に何百回もやり直させられている。
AIが軽率にprop/DB/viewを量産した結果、企業マスターDBはビューが約30個まで膨張した。

## 運用
- 原則は「**作る**」ではなく「**既存を使う・整える**」。追加前に必ず既存で代替できないか確認する。
- やむを得ず追加する場合は、(1) 承認を得る (2) **まとめて1回で**実施（小出し厳禁＝リセット回数を最小化）。
- Notionの表示/非表示・並び順・タブ配置・ビュー作成は**公開APIでは操作不可（UI専用）**。スキーマ確認/プロパティ追加は `ntn`(CLI)で、レイアウトはUI/ブラウザ操作で。
- 変更系のNotion操作は MCP ではなく `ntn` CLI を使う。
