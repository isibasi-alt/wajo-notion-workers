# 引き継ぎ正本 2026-06-08（A→B→商太 ライン実証セッション）

> 事実のみ。前提の全体像は `2026-06-08-ABShota-verification-map.md`、矢の設計は `specs/2026-06-08-arrow2-quiver-design.md`。

## 目的（北極星）
名刺1枚 → 営業がそのまま喋れる商談前トーク。3体AI（A=深掘り → B=与信 → 商太=とどめ）で企業を丸裸にし、商太がトークを返す。Notionを最強のデータソースにする。最終＝WAJO Sales OSを使える状態でリリース。判定＝営業が現場で使えるか。

## 決定済み事項
- **矢の軸**：「①相手の状況をズバリ当てて驚かす」が商談を進める瞬間（大ちゃん決定）。
- **A（深掘りリサーチ／Perplexity sonar）＝無料の主力**。`processCompanyResearchWebhook`で発火。**実機ライブ合格**（2026-06-08：実Perplexity19秒・出典50件超／未実在社では捏造せず「未確認法人」と正直判定／自社=和上はガードでNG）。
- **B（TDB与信／CosmosNet）＝技術完成・温存**。ロジック(tdb-fetcher/company-scoring)・取得部品(parse/matcher)テスト緑。`cosmosnet.mjs` の `MAPPED=true`（login/検索/一覧/購入確認/帳票マッピング完了）。**1社1,320〜1,760円(CCR+3,300)・管理職×有効取引先のみの課金奥の手**。`TDB_FETCHER_URL`未登録＝現状課金ゼロ。詰まりではない。
- **商太（3体目AI）＝喋れる本体が完成**。エンジン=`src/shouta-brief.ts`（`generateShoutaBrief`、LLMプロバイダ自動選択OpenAI→Perplexity、人格内蔵、`hits`=当てる弾配線済）。人格正本=`docs/assets/shouta/SHOUTA_PERSONA.md` v0.1。顔=`docs/assets/shouta/shouta.png`。
- **完全なライン実証済（2026-06-08）**：企業データ＋当てる弾 → 商太 → **Notionカード本文に商談前ブリーフ書込**。日本エコシステムのカード(`3784d017-81e7-81b0-8f2d-d72c200e9e8a`)で実際に書込・確認済。
- **2本目の矢＝「当てる弾の矢筒」**設計確定（`specs/2026-06-08-arrow2-quiver-design.md`）。approach C＝今Aを強化→gBizINFOを後で差す。矢筒順＝①再エネ/FIT満了 ②補助金 ③調達/入札 ④許認可/特許 ⑤求人 ⑥プレス ⑦代表の公開発信。

## 未決定事項（次の判断・実装待ち）
- **当てる弾Phase1**：A(`buildResearchQueries` in `src/index.ts`)に補助金/調達/許認可/求人/プレス観点を追加し、A出力を商太の`hits`へ自動配線（未着手＝本体変更のため慎重に）。
- **商太のWorker(index.ts)本統合＋トリガー接続**（名刺/企業webhook→A→商太→Notion書込の自動化）。現状はスタンドアロンmodule＋手動lineで実証のみ。
- **gBizINFOコネクタ**（Phase2／APIキー和上アカウントで申請中、届き次第"差すだけ"）。
- **法人番号API**申請（名寄せ土台・リードタイム長）。
- **社内ナレッジ配線**（`KNOWLEDGE_DATA_SOURCE_ID`/`AI_LEARNING_LOG_DATA_SOURCE_ID`＝商談/成約/失注/トーク → 商太が勝ちパターンを引用）。
- **商太のNotion上の"居場所"**（プロフィール/ホームページ）作成（深夜に試みたが未完）。

## 却下した案
- B（TDB）を「ブロッカー／未完」と扱う → 却下。設計通りの課金奥の手＝緑。
- 商太を商談準備レポートDBの飾りアイコンにする → 却下（前スレ大ちゃん指摘。商太＝AI本体の顔）。

## 重要な制約
- **Notion鉄板ルール**：プロパティ/DB/ビューを勝手に増やさない（要承認）。ページ作成・本文書込はOK。変更系はMCPでなく `ntn` CLI。
- **実課金(TDB)・金融実行は確認必須**。勝手に課金しない。
- 個人情報・契約情報を外部送信しない。代表個人情報は公開情報のみ。出典明示・推測は明示・捏造しない。
- **キー検証手順**：`ntn workers env pull --file .env.worker.tmp` で一時取込→tsxハーネス→**即削除**（既存`.env`17行は壊さない）。Worker envに `PERPLEXITY_API_KEY`/`WAJO_OPENAI_API_KEY` 有効（`OPENAI_API_KEY`は空）。
- ツール送信が壊れる事象が頻発（`court`化）。Bash/Write は1つずつ確実に。
- **大ちゃんはCL版（Claudeのアプリ版）利用＝回答中のファイルリンク/ローカルパスが飛べない。** 成果物を見せる時は必ず `SendUserFile` で実ファイルを送る（md/コード/画像）。Notion等はURLで示す。パスは参照用に併記のみ。
- **ダミーデータ（リリース前に必ず削除）**：再エネ丸裸DBにダミー5社（削除リスト=`~/wajo-tdb-fetcher/_DUMMY_cards_to_delete_2026-06-08.txt`）。日本エコシステムカードの商太ブリーフ本文もダミー検証物。

## 現在の成果物（ディスク上）
- `src/shouta-brief.ts`（商太エンジン）
- `docs/assets/shouta/SHOUTA_PERSONA.md`（人格v0.1）／`docs/assets/shouta/shouta.png`(顔)
- `docs/superpowers/2026-06-08-ABShota-verification-map.md`（全体フロー図＋A/B/商太の検証結果）
- `docs/superpowers/specs/2026-06-08-arrow2-quiver-design.md`（2本目の矢の設計）
- `~/wajo-tdb-fetcher/`（FIT丸裸エンジン・大阪府索引2,817社）／ダミー検証物（再エネ丸裸DB5社＋日本エコにブリーフ書込）

## 次にやるべき一手（順）
1. **当てる弾Phase1**：A(`buildResearchQueries`)に補助金/調達/許認可/求人/プレス観点を追加 → A出力を商太`hits`へ自動配線。
2. **商太をWorker統合＋トリガー接続**：名刺/企業webhook → A → 商太 → Notion書込を自動化（＝完全ラインの本番化）。
3. **gBizINFOコネクタ**実装（APIキー到着後）／**法人番号API**申請。
4. **社内ナレッジ配線**で商太に勝ちパターン引用を追加。
5. リリース前に**ダミーデータ削除**。
