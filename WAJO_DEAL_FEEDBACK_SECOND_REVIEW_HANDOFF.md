# WAJO Sales OS｜商談フィードバック二次レビュー Worker 実装引き継ぎ

## 目的

商談管理DBに入っている一次フィードバックを、ChatGPTで定性レビューし直す。

これは一次AIの置き換えではなく、営業スコア、営業フィードバック、改善ポイント、次回トークイメージ、フォローメールヒント、成約へのヒントの品質を横から補強する二次レビューである。

## 方針

- 初期運用は手動起動にする。
- 高価値商談、重要商談、違和感のある商談だけを対象にする。
- 一次AIの出力は上書きしない。
- 商談ステータス、成約、案件、タスク、評価確定は更新しない。
- 外部送信する場合は、Worker側で送信内容を最小化する。

## 推奨Notionプロパティ

対象DB: 商談管理DB

- 二次FBステータス
  - 未処理
  - 二次レビュー依頼
  - レビュー済
  - 要確認
  - 対象外
  - エラー
- 二次レビューコメント
- フィードバック品質
  - 良い
  - 要修正
  - 情報不足
- 二次レビュー更新日時
- 二次レビュー最終処理商談ID
- 外部レビュー可否

初期リリースでは既存プロパティを壊さず、存在するプロパティだけ safeUpdateExistingProperties で更新する。

## Workerに追加する能力

### Tool

`processDealFeedbackSecondReviewById`

入力:

- dealPageId: 商談管理DBのページID
- dryRun: trueならNotionを書き換えない

### Webhook

`processDealFeedbackSecondReviewWebhook`

入力候補:

- dealPageId
- pageId
- page_url
- entity.id

pageIdが取れない場合は、初期実装では無理に検索せずエラーで返す。

## 読む内容

商談管理DB:

- 商談名
- 商談日
- 商談概要
- 商談回数
- 商談ステータス
- 関連企業
- 関連会議
- 営業スコア
- 営業フィードバック
- 改善ポイント
- 次回トークイメージ
- フォローメールヒント
- 成約へのヒント

関連会議が取れる場合:

- 要約
- 議事内容
- 決定事項
- アクション項目
- テキスト

## ChatGPTへ渡す内容

原則として、商談ページの構造化情報と一次AI出力だけを渡す。

個人名、電話番号、メールアドレス、契約詳細、機密金額は初期実装では渡さない。必要な金額は「費用懸念あり」「粗利条件が論点」などに要約する。

## ChatGPTへの役割

あなたは和上ホールディングスの営業フィードバック二次レビュアーです。

一次AIが返した営業フィードバックを、営業現場で本当に次の商談に使えるかという観点でレビューしてください。

レビュー観点:

1. フィードバックが具体的か
2. 次回トークイメージがそのまま話せるレベルか
3. 改善ポイントが行動に落ちているか
4. 成約へのヒントが商談状況に合っているか
5. 根拠が薄い断定や、言い過ぎがないか
6. もっと踏み込むべき質問があるか

禁止:

- 最終評価、総合評価、給与、処遇判断をしない
- 商談ステータスや成約判断を確定しない
- タスクを作らない
- 人格評価をしない
- 不明なことを断定しない

返却JSON:

```json
{
  "quality": "良い | 要修正 | 情報不足",
  "summary": "二次レビューの要約",
  "strongPoints": ["良い点"],
  "revisionSuggestions": ["修正提案"],
  "nextTalkUpgrade": "次回トークイメージをさらに良くする一言",
  "riskNotes": ["注意点"],
  "recommendedStatus": "レビュー済 | 要確認"
}
```

## 書き戻し

存在するプロパティだけ更新する。

- 二次FBステータス: recommendedStatus
- 二次レビューコメント: summary + strongPoints + revisionSuggestions + nextTalkUpgrade + riskNotes
- フィードバック品質: quality
- 二次レビュー更新日時: 現在時刻
- 二次レビュー最終処理商談ID: 対象商談ID

一次AIの以下プロパティは初期実装では上書きしない。

- 営業スコア
- 営業フィードバック
- 改善ポイント
- 次回トークイメージ
- フォローメールヒント
- 成約へのヒント

## 環境変数

- OPENAI_API_KEY
- OPENAI_MODEL
- WAJO_WORKER_SECRET

Nizi.aiを使う場合は、Nizi.ai側のAPI仕様、送信データの扱い、ログ保存、学習利用有無を確認してから、Worker内のChatGPT呼び出し部分を差し替える。

## 実装上の注意

- 既存の `processCompanyResearchById` / `processMeetingPrepReportWebhook` と同じ型・構成に寄せる。
- Notion更新は `safeUpdateExistingProperties` を使う。
- API失敗時は `二次FBステータス = エラー`、`二次レビューコメント` にエラー要約を残す。
- dryRunで、送信予定payloadと返却予定の要約が分かるようにする。

## 完了条件

- dryRunで商談1件を読める。
- 実行時にChatGPTからJSONが返る。
- 商談DBの二次レビュー欄だけが更新される。
- 一次フィードバック欄は上書きされない。
- 商談ステータス、成約、案件、タスク、評価関係は更新されない。
- 再実行しても二次レビュー欄の更新に留まり、重複ページや重複タスクを作らない。
